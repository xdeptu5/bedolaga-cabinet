name: Auto Sync Fork

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    
    steps:
      - name: Checkout target repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync logic
        id: sync_step
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          OLD_HASH=$(git rev-parse HEAD)
          
          git remote add upstream https://github.com/BEDOLAGA-DEV/bedolaga-cabinet.git
          git fetch upstream main
          
          git merge upstream/main --no-edit || (echo "CONFLICT" && exit 1)
          
          NEW_HASH=$(git rev-parse HEAD)
          
          if [ "$OLD_HASH" != "$NEW_HASH" ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
            git push origin main
            echo "Обнаружены новые изменения."
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
            echo "Изменений нет, синхронизация не требуется."
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Trigger Deploy Pipeline
        if: steps.sync_step.outputs.has_changes == 'true'
        run: |
          echo "Запускаю сборку..."
          gh workflow run deploy.yml --ref main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send Notification
        # Условие: только если есть изменения ИЛИ если возникла ошибка
        if: steps.sync_step.outputs.has_changes == 'true' || failure()
        run: |
          if [ "${{ job.status }}" = "failure" ]; then
            ICON="❌"
            STATUS="Sync Failed (Conflict)"
          else
            ICON="✅"
            STATUS="Sync & Deploy Triggered"
          fi

          MESSAGE="<b>Safexess VPN Bot</b>%0A$ICON <b>$STATUS</b>%0A%0A📦 <b>Repository:</b> ${{ github.repository }}"
          
          curl -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
          -d "message_thread_id=${{ secrets.TELEGRAM_TOPIC_ID }}" \
          -d "parse_mode=HTML" \
          -d "text=$MESSAGE"
